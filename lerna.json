{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": {
    "run": {
      "npmClient": "yarn"
    },
    "publish": {
      "ignoreChanges": [
        "ignored-file",
        "*.md",
        "package-lock.json",
        "yarn.lock"
      ]
    }
  }
}
