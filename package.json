{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.22.1"
    },
    "scripts": {
        "publish": "node ./scripts/updateReadme.js && node ./scripts/removePackageLock.js && lerna publish"
    }
}
